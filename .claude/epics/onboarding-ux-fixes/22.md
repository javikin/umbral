---
name: improve-category-ux
status: closed
priority: high
github: https://github.com/javikin/umbral/issues/22
created: 2026-01-04T23:34:58Z
updated: 2026-01-04T23:34:58Z
depends_on: []
parallel: true
---

# Task 4: Mejorar UX de Categorías

## Problema

Los tags/chips de categorías en el selector de apps no son claros ni están en español.

## Solución

### 1. Actualizar Strings de Categorías

```xml
<!-- strings.xml -->
<string name="category_all">Todas</string>
<string name="category_social">Redes Sociales</string>
<string name="category_games">Juegos</string>
<string name="category_productivity">Productividad</string>
<string name="category_entertainment">Entretenimiento</string>
<string name="category_communication">Comunicación</string>
<string name="category_shopping">Compras</string>
<string name="category_news">Noticias</string>
<string name="category_system">Sistema</string>
<string name="category_other">Otras</string>
```

### 2. Agregar Iconos a Categorías

```kotlin
enum class AppCategory(
    val displayName: Int,
    val icon: ImageVector
) {
    ALL(R.string.category_all, Icons.Default.Apps),
    SOCIAL(R.string.category_social, Icons.Default.People),
    GAMES(R.string.category_games, Icons.Default.SportsEsports),
    PRODUCTIVITY(R.string.category_productivity, Icons.Default.Work),
    ENTERTAINMENT(R.string.category_entertainment, Icons.Default.Movie),
    COMMUNICATION(R.string.category_communication, Icons.Default.Chat),
    SHOPPING(R.string.category_shopping, Icons.Default.ShoppingCart),
    NEWS(R.string.category_news, Icons.Default.Newspaper),
    SYSTEM(R.string.category_system, Icons.Default.Settings),
    OTHER(R.string.category_other, Icons.Default.MoreHoriz)
}
```

### 3. Actualizar CategoryChip Component

```kotlin
@Composable
fun CategoryChip(
    category: AppCategory,
    selected: Boolean,
    onClick: () -> Unit
) {
    FilterChip(
        selected = selected,
        onClick = onClick,
        label = {
            Row(verticalAlignment = Alignment.CenterVertically) {
                Icon(
                    imageVector = category.icon,
                    contentDescription = null,
                    modifier = Modifier.size(16.dp)
                )
                Spacer(modifier = Modifier.width(4.dp))
                Text(stringResource(category.displayName))
            }
        }
    )
}
```

### 4. Categorización de Apps

```kotlin
fun categorizeApp(packageName: String, appName: String): AppCategory {
    return when {
        // Redes Sociales
        packageName.contains("facebook") ||
        packageName.contains("instagram") ||
        packageName.contains("twitter") ||
        packageName.contains("tiktok") ||
        packageName.contains("snapchat") -> AppCategory.SOCIAL

        // Juegos
        packageName.contains("game") ||
        packageName.contains("play") -> AppCategory.GAMES

        // Comunicación
        packageName.contains("whatsapp") ||
        packageName.contains("telegram") ||
        packageName.contains("messenger") -> AppCategory.COMMUNICATION

        // ... más reglas

        else -> AppCategory.OTHER
    }
}
```

## Archivos a Modificar

- `app/src/main/res/values/strings.xml`
- `app/src/main/java/com/umbral/domain/apps/AppCategory.kt`
- `app/src/main/java/com/umbral/presentation/ui/screens/apps/AppSelectorScreen.kt`
- `app/src/main/java/com/umbral/presentation/ui/components/CategoryChip.kt`

## Diseño Visual

```
┌────────────────────────────────────────┐
│ Seleccionar apps a bloquear           │
├────────────────────────────────────────┤
│                                        │
│ [📱 Todas] [👥 Redes] [🎮 Juegos] ... │
│                                        │
│ ┌────────────────────────────────────┐ │
│ │ 📘 Facebook           ☐            │ │
│ │ com.facebook.katana                │ │
│ └────────────────────────────────────┘ │
│ ┌────────────────────────────────────┐ │
│ │ 📷 Instagram          ☑            │ │
│ │ com.instagram.android              │ │
│ └────────────────────────────────────┘ │
│                                        │
└────────────────────────────────────────┘
```

## Criterio de Done

- [ ] Categorías en español claro
- [ ] Cada categoría tiene icono
- [ ] Filtro por categoría funciona
- [ ] Apps se categorizan automáticamente
- [ ] UX intuitiva y limpia
