---
name: Crear Componentes UI Base
status: closed
created: 2026-01-05T03:15:00Z
updated: 2026-01-05T03:15:00Z
github:
parallel: true
depends_on: [001]
effort: 8h
---

# Task: Crear Componentes UI Base

## Descripción

Crear componentes reutilizables que implementen el sistema de diseño y se usen en toda la app.

## Archivos a Crear

### `app/src/main/java/com/umbral/presentation/ui/components/`

1. **UmbralButton.kt**
```kotlin
@Composable
fun UmbralButton(
    text: String,
    onClick: () -> Unit,
    modifier: Modifier = Modifier,
    enabled: Boolean = true,
    variant: ButtonVariant = ButtonVariant.Primary,
    loading: Boolean = false
) {
    // Press animation: scale 0.95
    // Loading: show spinner inline
    // Variants: Primary, Secondary, Outline, Ghost
}

enum class ButtonVariant { Primary, Secondary, Outline, Ghost }
```

2. **UmbralCard.kt**
```kotlin
@Composable
fun UmbralCard(
    modifier: Modifier = Modifier,
    elevation: UmbralElevation = UmbralElevation.subtle,
    shape: Shape = MaterialTheme.shapes.large,
    onClick: (() -> Unit)? = null,
    content: @Composable ColumnScope.() -> Unit
) {
    // Press state: elevation change
    // Consistent padding
    // Optional click behavior
}
```

3. **UmbralChip.kt**
```kotlin
@Composable
fun UmbralChip(
    label: String,
    selected: Boolean,
    onClick: () -> Unit,
    modifier: Modifier = Modifier,
    leadingIcon: @Composable (() -> Unit)? = null
) {
    // Selected: filled with primary color
    // Unselected: outlined
    // Animation on selection change
}
```

4. **UmbralToggle.kt**
```kotlin
@Composable
fun UmbralToggle(
    checked: Boolean,
    onCheckedChange: (Boolean) -> Unit,
    modifier: Modifier = Modifier,
    enabled: Boolean = true
) {
    // Slide animation with bounce
    // Color transition
    // Haptic feedback integration
}
```

5. **AnimatedIcon.kt**
```kotlin
@Composable
fun AnimatedIcon(
    icon: ImageVector,
    modifier: Modifier = Modifier,
    tint: Color = LocalContentColor.current,
    animate: Boolean = true
) {
    // Subtle scale/rotation animation on appear
    // For use in buttons, cards, etc.
}
```

6. **StreakDisplay.kt**
```kotlin
@Composable
fun StreakDisplay(
    currentStreak: Int,
    modifier: Modifier = Modifier,
    showMiniCalendar: Boolean = true
) {
    // Large streak number
    // Fire icon with subtle animation
    // Mini calendar of last 7 days
    // Celebration on milestones (7, 30, 100)
}
```

7. **StatsGraph.kt**
```kotlin
@Composable
fun StatsGraph(
    data: List<Float>,
    modifier: Modifier = Modifier,
    lineColor: Color = MaterialTheme.colorScheme.primary,
    showLabels: Boolean = true
) {
    // Minimalist line graph
    // Animated drawing on appear
    // Touch to show value
}
```

8. **LoadingIndicator.kt**
```kotlin
@Composable
fun LoadingIndicator(
    modifier: Modifier = Modifier,
    size: Dp = 48.dp
) {
    // Custom loading animation
    // Uses primary color
    // Subtle pulsing effect
}
```

## Criterios de Aceptación

- [ ] Todos los componentes creados con previews
- [ ] Animaciones smooth a 60fps
- [ ] Componentes respetan tema light/dark
- [ ] Props bien documentados
- [ ] Estados de loading/disabled funcionando

## Previews Requeridos

Cada componente debe tener:
- Preview en light mode
- Preview en dark mode
- Preview de diferentes estados (enabled, disabled, loading)
- Preview de diferentes variantes

## Notas

- Usar `animateAsState` para transiciones suaves
- Preferir `Modifier.animateContentSize()` para cambios de tamaño
- Agregar haptic feedback donde tenga sentido
